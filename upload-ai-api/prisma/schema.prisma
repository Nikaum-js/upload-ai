// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "postgres://Nikaum-js:D5jbJs0qPEAX@ep-flat-surf-79688622.us-east-2.aws.neon.tech/neondb"
}

model Video {
  id            String   @id @default(uuid())
  name          String
  patch         String
  transcription String?
  created_at    DateTime @default(now())
}

model Prompt {
  id       String @id @default(uuid())
  title    String
  template String
}
